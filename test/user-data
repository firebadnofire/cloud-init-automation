#cloud-config

package_update: true
package_upgrade: false

packages:
  - curl
  - bash
  - ca-certificates
  - gcc
  - binutils
  - pkg-config

runcmd:
  - mount --mkdir -o ro -L cidata /mnt/cidata
  - /mnt/cidata/test.sh
